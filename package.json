{
  "name": "f1-driver-search",
  "version": "1.0.0",
  "description": "Projeto completo React + API OpenF1",
  "scripts": {
    "start": "node backend/f1.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": "18.x"
  }
}

  
